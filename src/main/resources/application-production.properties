# Hosting credentials must be supplied by the platform's secret manager.
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}

# A production deployment is expected to run behind an HTTPS reverse proxy.
tradingsim.security.require-https=true
server.servlet.session.cookie.secure=true
