/**
 * Deterministic clock and event-queue primitives for backtesting.
 *
 * <p>Events are processed by timestamp and then insertion order, making the
 * same input data produce the same output on every run.</p>
 */
package com.tradingsim.engine;
