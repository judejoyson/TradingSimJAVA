/**
 * User-implemented strategy extension points and small reference strategies.
 *
 * <p>A strategy receives one market-data point at a time and submits orders
 * only through its provided context, which keeps it independent of matching
 * and portfolio implementations.</p>
 */
package com.tradingsim.strategy;
