/**
 * REST endpoints and consistent API error responses for browser clients.
 *
 * <p>Controllers translate HTTP requests into service calls. Business rules
 * should remain in services so they can be tested without a web server.</p>
 */
package com.tradingsim.web;
