/**
 * In-memory paper-account models and services.
 *
 * <p>This package owns cash, positions, average cost, trade history, and
 * mark-to-market account views. It belongs to the earlier live-quote workflow;
 * ReplayLab's interactive historical order state currently lives in
 * {@code static/chart.js} because each replay is local to one browser tab.</p>
 */
package com.tradingsim.account;
